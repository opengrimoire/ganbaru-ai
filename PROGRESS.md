# Progress

## Current phase

Phase 1 — Core loop

## Status

Building the core plan-focus-earn-see cycle. Monorepo scaffold, app shell, Pomodoro timer, Kanban board, SQLite schema, calendar, and XP pipeline are done. Skill tree rebuilt with Shield Hero-style center-snap navigation replacing D3.js force graph.

## Completed

- Phase 0: created CLAUDE.md, ROADMAP.md, PROGRESS.md, DECISIONS.md
- Phase 0: designed 12-phase roadmap covering all product spec systems, verified coverage
- Phase 1: monorepo scaffold (Turborepo + pnpm workspaces + Cargo workspace)
- Phase 1: Tauri v2 + Svelte 5 + Vite app (plain Svelte, not SvelteKit)
- Phase 1: shadcn-svelte + Tailwind CSS v4 with notion-like neutral gray theme (dark + light)
- Phase 1: custom in-app title bar (decorations: false, drag region, window controls)
- Phase 1: shared-types package (Task, SessionBlock, SkillNode, XpEntry types)
- Phase 1: SQLite schema (tasks, session_blocks, skill_nodes, skill_branches, pomodoro_sessions, xp_entries, streaks, daily_xp_caps)
- Phase 1: app shell with sidebar navigation (calendar, kanban, pomodoro, skill-tree views)
- Phase 1: Pomodoro timer (focus/short_break/long_break phases, cycle tracking, start/pause/reset/skip)
- Phase 1: Kanban board with CRUD (backlog/todo/in_progress/done columns, task creation, move between columns, delete, priority badges)
- Phase 1: calendar view (custom Google Calendar-style component, week/day/month views, session block CRUD, drag-and-drop move/resize, current-time indicator, SQLite-backed — replaced schedule-x library)
- Phase 1: skill tree view (SVG + Svelte center-snap navigation, neighborhood culling, sub-layer navigation, keyboard nav, locked/available/unlocked visual states, node detail panel, removed D3.js dependency)
- Phase 1: skill tree data (153-node temporal tree from JSON, 7 branches with radial tree layout adapter, cluster sub-graphs, auto-focus, always-visible detail panel)
- Phase 1: XP pipeline (Pomodoro → pomodoro_sessions + xp_entries in SQLite, +XP toast on completion)

## In progress

- UI overhaul: sidebar replaced with tab navigation, contour layout, dual theme (light/dark), custom calendar
- Pomodoro: native pre-break notification and fullscreen break overlay (Linux implemented, Windows/macOS pending)
- Pomodoro: centralized timeline rail with focus fills and break bands
- Pomodoro: suspend/wake detection, pause gap visualization, orphan cleanup, idle detection with forced-interaction overlay

## Blocked / needs decision

(none)

## Next up

Phase 2 — Notes and Diary:
- Tiptap rich text editor integration (markdown storage)
- Morning and evening diary entry forms
- Note creation, editing, and linking
- Bidirectional backlink index in SQLite

## Session log

| Date | Summary |
|---|---|
| 2026-03-09 | Created tracking documents and 12-phase roadmap. Phase 1 complete: monorepo scaffold, Tauri+Svelte, SQLite, app shell, Pomodoro timer, Kanban board, calendar with session blocks, XP pipeline. Custom title bar + Notion-like theme. Skill tree rewritten: replaced D3.js force graph with SVG + Svelte center-snap navigation (Shield Hero style), neighborhood culling, sub-layer navigation, authored graph data, keyboard controls, visual states. Updated PRODUCT_SPEC, TECH_STACK, ROADMAP, DECISIONS, CLAUDE.md. Integrated 153-node temporal skill tree JSON (7 branches, radial layout adapter, cluster sub-graphs). Fixed: center-snap positioning, kanban/calendar scroll, auto-focus, always-visible detail panel, simplified keyboard nav. |
| 2026-03-11 | UI overhaul: replaced sidebar with tab navigation, contour layout, dual light/dark theme with custom colors. Fixed Ctrl+Shift+Tab (WebKitGTK e.code workaround). |
| 2026-03-12 | Replaced schedule-x calendar library with custom Google Calendar-style component: week/day/month views, event overlap layout, pointer-based drag-and-drop, current-time indicator, half-hour dashed gridlines, 8-color event palette. Undo/redo (Ctrl+Z/Y) with confirmation dialogs. Clickable day headers, Alt+arrow view history, arrow key navigation. Header drill-down picker, help/settings buttons, calendar account selector. Mini calendar scroll navigation, month view past-day styling. Scrollable top bar for tab switching. |
| 2026-03-13 | Pomodoro: added native pre-break notification (notify-rust, action buttons, transient, sound), fullscreen break overlay (GTK, countdown timer, space x3 dismiss, Esc x3 skip, 30s inactivity re-show via GNOME IdleMonitor). Keyboard shortcut blocking during overlay via gsettings (Super, Alt, dash-to-dock hotkeys). System tray progress ring with pause/skip controls. Close confirmation dialog. Theme persistence. Pomodoro tab removed — timer now auto-starts from active calendar session blocks. |
| 2026-03-14 | Calendar: drag-to-create blocks (click+drag with 10-min grid snap), snap indicator line with time label, resize flip (drag past opposite edge), block hover highlight. Extracted shared drag controller (useDragController.svelte.ts) for WeekView/DayView. Fixed calendar time storage (local time, no UTC conversion). Scroll position preserved across view switches. Break overlay focus handling. Added vitest with 38 unit tests. Provisional DB reset button. Close shortcut Ctrl+Shift+W. Hour height 1.4x. RAM usage in title bar (dev monitoring). Resolved all Svelte a11y warnings. Fixed sub-pixel gridline/text rendering, gridline color artifact during drag, global cursor lock during drag. |
| 2026-03-15 | Pomodoro: removed "work 3 more minutes" from break screen, notification changed to "Focus session ending in 1 minute" with one-time +3min extension (removed skip-break option). Break overlay changed to black background with white text. RAM usage now includes WebKitGTK child processes. Reset button now deletes database file and restarts app. Fixed DB connection race condition and silent load failures. Focus duration default set to 40 minutes. |
| 2026-03-16 | Calendar: replaced CreateDialog/EventDetail with floating EventPanel (inline edit with color picker, description, pomodoro settings). Added repeat rule (daily/weekdays/weekly/monthly/yearly) and notification scheduling (native OS notifications via notify-rust, 30s polling). DB migration v2 (color, description columns), v3 (exceptions, repeat_until). Recurring events: virtual instance expansion, Google Calendar-style scope dialog (this/following/all) for save/delete/drag. Detach instance, split series, add exception store methods. Live preview for non-recurring edits only. Neutral create preview (no default blue). Fixed Svelte 5 reactivity issue with plain let vs $state for onChange tracking. DB migration v4: renamed session_blocks to calendar_events, added timezone/calendar_id columns, renamed repeat_rule to rrule (iCal-style), extracted pomodoro config into normalized pomodoro_configs table, recreated skill branch junction table, added indexes on start_time/end_time/calendar_id. Refactored CalendarEvent type (PomodoroConfig nested object, timezone/calendarId fields). Added rrule.ts conversion functions with tests. Refactored calendar store addBlock from 8 positional args to options object. Updated EventPanel, CalendarView, App.svelte for new schema. Fixed recurring event duplication bug: expandRecurring now skips templates whose start date is past repeat_until (caused ghost events when splitting at first instance). Scope-first recurring event editing: replaced post-save scope dialog with segmented scope selector (this/following/all) at top of EventPanel. Live preview now works for recurring events under all scopes via store-level snapshot/restore system (saveSnapshot, restoreSnapshot, previewRecurring). Switching scope re-previews in real-time. Drag-only scope dialog remains for drag operations. Custom recurrence system: replaced simple RepeatRule presets with RecurrenceConfig (frequency, interval, weekdays, end conditions: never/until/count). Full rrule.ts rewrite with bidirectional iCal RRULE conversion, preset detection, label formatting. Store updated: advanceDate handles intervals and BYDAY weekday sets, expandRecurring supports COUNT. EventPanel redesigned with toggle group sections: Pomodoro (preset radio buttons: auto/deep/creative/extended/custom), Notifications (multi-select presets + 2 custom slots), Repeat (interval/freq/weekday picker/end conditions), Music (placeholder). DB migration v5: notification_minutes column replaced with notifications TEXT (JSON array) for multi-notification support. Draggable EventPanel via pointer capture on accent bar. |
| 2026-03-18 | EventPanel polish: Inter font, dark mode color refinement (--panel-bg #282A2C, --panel-contrast #1E1F20, --foreground #C4C7C5, --muted-foreground #9EA1A0), emerald highlights across all sections (no ring/contour), individual rounded cards per feature section with gaps. Date picker rewritten with drill-down/wheel scroll matching sidebar mini-calendar. Time slots changed to 30-min intervals. Frequency selector as emerald pills, weekday picker full-width 3-letter labels with auto-selection. Recurrence mini calendar with drill-down. Save button darker green in dark mode. Live preview for date/time changes in create mode. Title separator line, color selector moved to bottom, section icon border removed. Light mode colors: placeholder #444746, date/time #1F1F1F, dark mode date/time #E3E3E3. Removed font-semibold from day column headers in week/day/month views. Reduced panel shadow. Recurrence "Ends" section redesigned: Google Calendar-style layout with always-visible On date box and After count box, aligned columns, floating date picker (fixed positioning to avoid clipping), editable date input with manual entry support. Frequency pills always plural. Dark mode section hover uses darker highlight (black/20). |
| 2026-03-19 | EventPanel UX: complete de-greening (replaced emerald indicators/highlights/hovers with neutral bg-black/5 dark:bg-black/15, indicator fills #6B6F6E light / bg-foreground dark), unified hover/selected visual language across all interactive elements. Custom notifications redesigned as compact inline rows with floating dropdown. Custom Pomodoro with full labels and narrower boxes. Link insertion: replaced prompt() with inline floating popover, Ctrl+V link wrapping on selected text. Faint background on unselected weekday pills. Input box text darker (#1F1F1F / #E3E3E3). Panel positioning: pinned base position via $effect + untrack(panelHeight) so expanding sections grow downward without shifting top, overflow correction nudges top up only when bottom hits viewport. Fixed drag-near-bottom dead zone (was caused by double-clamping). Discard unsaved changes: "Discard unsaved changes?" modal on close when dirty (reuses CalendarView confirm system with configurable labels, z-60 overlay above panel). Event block remains interactive while panel is open (editingEventId prop threaded through WeekView/DayView/DayColumn/EventBlock, z-index 45 above backdrop). Reactive date/time sync from event prop so drag/resize updates panel fields live. Current-time line z-index bumped to z-46 (above editing blocks at z-45). Create mode block drag/resize: pseudo CalendarEvent injected into events list, editingEventId set to PENDING_CREATE_ID, handleEventUpdate routes pseudo-event back to pendingCreatePreview, time sync effect extended for create mode. Preview visualization system: static diagonal stripe overlay (::after pseudo-element with repeating-linear-gradient) marks unsaved/editing events. Drag preview replaces original block (draggingEventId hides source, conditioned on dragPreview to preserve click). Series-wide preview: editingTemplateId prop marks all recurring siblings, not just the selected instance. Create-mode recurrence preview via exported expandRecurring. Drag flash fix: previewBlock() called synchronously before async updateBlock. Snap line fix: cleared on drag start, mousemove guarded during drag. Past events use CSS filter (saturate 0.7, brightness 0.9) instead of opacity. Removed hover effects from event blocks. Z-index hierarchy: blocks(1/45), current-time(46), drag-preview(46), snap-line(47), sticky-headers(48), panel(50), confirm(60). |
| 2026-03-20 | EditSession architecture: replaced snapshot/restore preview system with pure overlay model. New files: edit-session.svelte.ts, display-events.ts, recurrence.ts. CalendarView rewritten with EditSession + displayResult derived. 15 new unit tests for display-events (96 total). Event color system: replaced 8-color palette with 20 colors (ruby/coral/tangerine/amber/honey/lime/emerald/jade/teal/cyan/sky/azure/indigo/violet/purple/orchid/rose/blush/slate/sage), universal text colors per theme, accent bar replaces left border. Event block icons (repeat, bell) repositioned to top-right corner aligned with title baseline. Pomodoro segment tracking: collapsed all DB migrations into single v1 with pomodoro_segments table, computePlannedSegments pure utility with 14 tests (110 total), segment lifecycle in store (create/advance/stop/skip/overtime), accent bar break band rendering (planned at 40% opacity, active with pulse, completed as bg-color gaps, skipped hidden), cross-midnight band clipping, break overtime reactivity via counter interval. Extracted ConfirmDialog component. Reset database closes app instead of restarting. |
| 2026-03-21 | Pomodoro mid-session reconfiguration: changing pomodoro preset while timer is running adjusts current phase remaining time (elapsed preserved), marks old segments completed/skipped, creates new segment plan (bridge segment + future segments with correct cycle progression). Handles edge cases: elapsed exceeds new duration (immediate phase advance), paused state, break overtime with extended break duration. Fixed current-time indicator circle clipped by overflow-hidden (moved indicator outside clipping container as sibling). Removed saturate/brightness filter from past event blocks (calendar background dimming retained). Focus time and cycle count inheritance across adjacent blocks: computePlannedSegments accepts initialFocusOffsetMinutes and initialCycleNumber so successor blocks continue where predecessors left off. computeTrailingFocusMinutes and computeTrailingCycleNumber determine carryover state. DayColumn pre-computes inheritance chain for planned view accent bar bands. transitionToBlock preserves focus continuity at block boundaries. findActiveBlock prefers currently active block when multiple overlap. 23 new tests (133 total). Centralized timeline rail: replaced per-event accent bar break bands with single 6px rail on DayColumn left edge. computeDayTimelineBands pure function handles containment filtering, cursor-based inheritance, active/persisted segment projection. Rail shows break bands (gray) and focus fills (green) from actual pomodoro sessions. Separate rail segments for non-contiguous event groups. EventBlock simplified to flat colored rectangles. Fixed DB load bug (stale notification_minutes column). 21 new tests (154 total). Fixed current-time indicator alignment: added seconds-level precision (fractional minutes, 1s update interval) and absolute positioning so red line, past overlay, and rail fills all meet at the same pixel. |
| 2026-03-22 | Pomodoro suspend/wake detection: tick-gap heuristic (5s threshold) detects laptop suspend, records synthetic pause interval, re-anchors phaseEndTime, shows resume/stop dialog on wake. Auto-stops if event ended during suspend. Pause gap visualization: pause_log DB column tracks pause intervals per segment, splitAroundPauses splits green fill bar around pauses (gaps visible in timeline rail). Orphan cleanup: cleanupOrphans marks abandoned active segments as interrupted on startup. Overnight fixes: activeBlockEndMs auto-stop in tick, overflow-hidden on rail segments, visibilitychange listeners for immediate UI update. Gridline positioning respects rail area. Break line reactivity during pause (void currentTimeMinute dependency). Block resize detection triggers new break marks. 2 new tests (156 total). Idle detection: Rust get_idle_status command (GNOME IdleMonitor D-Bus for input idle ms + /proc/*/fd webcam-in-use check), pomodoro store polls every 15s during focus, auto-pauses with synthetic pause interval when idle exceeds threshold and no webcam active, resume/stop dialog. idleTimeoutMinutes field added to PomodoroConfig/DB (nullable, default 3min for testing). EventPanel "Pause on idle" checkbox in pomodoro section (on by default). Guards on tray buttons, keyboard shortcuts, and checkActiveBlock during idle pause. Cross-platform idle detection: Windows (GetLastInputInfo + registry webcam check via PowerShell), macOS (ioreg HIDIdleTime + pgrep VDCAssistant/AppleCameraAssistant/appleh13camerad), Linux non-GNOME fallback (xprintidle for X11). Added winapi 0.3 dependency for Windows. GTK idle overlay for Linux: fullscreen forced-interaction screen matching break overlay pattern (multi-monitor GTK windows, keyboard capture, GNOME shortcut disabling, screensaver inhibition, alert sounds every 15s, system notification, Space to resume, Esc to stop). Svelte IdleOverlay as fallback for non-Linux (Tauri window fullscreen + always-on-top). Dual-overlay conflict resolved: show_idle_overlay returns bool, Svelte component skips sounds/notifications/fullscreen when native overlay is active. dismissedBlockId prevents session restart after explicit idle/suspend stop. |
| 2026-03-23 | Idle overlay fixes: GTK timer callback now stops on dismiss (was returning ControlFlow::Continue indefinitely, causing alert sounds to keep playing after Space/Esc). Moved dismissedBlockId from App.svelte local state into pomodoro store so GTK event listeners (idle-overlay-stop, idle-overlay-resume) can set it before clearing activeBlockId, preventing checkActiveBlock from restarting a stopped session. Idle timeout default changed to 1 minute for testing. EventPanel "Pause on idle" restyled from native checkbox to notification-style selector (rounded indicator dot, matching font and hover/selected states). Calendar UX polish: replaced pulse animation on editing/preview events with contour + glow effect (::after pseudo-element with semi-transparent border, box-shadow glow). Recurring event chain drag blocked (canDrag callback in useDragController), click on chain event shows discard dialog if dirty. applyFollowing fixed to only preview virtual template instances. ConfirmDialog restyled (lighter backdrop, larger text/buttons, borderless cancel, blur on Esc). EventPanel: panel prefers left-side positioning, horizontal alignment unified (px-1), title overflow fade gradient, scope selector emerald highlight with "Only this" label, tighter spacing between sections, time picker nearest-slot highlight (bold + foreground color), scroll fade via CSS mask-image, end picker positioned at 50% with duration labels "(X.Y hrs)", cross-midnight save bumps end date instead of swapping times, reduced picker widths and padding. |
| 2026-03-24 | Fixed current-time line and past-day shadow not updating after laptop wake: isToday/isPastDay used non-reactive new Date() calls that Svelte couldn't track; replaced with reactive todayStr state updated in updateCurrentTime (runs every second + on visibilitychange). Fixed all 51 pre-existing svelte-check errors: added missing @ganbaruai/shared-types workspace dependency to apps/desktop (SkillNode type was unresolved, cascading to VisibleNode), exported VisibleNode/VisibleEdge interfaces, fixed db.ts QueryResult.lastInsertId undefined narrowing. UI polish: added contour shadow to content panel (1px spread + soft blur, dark mode aware) and active tab (1px spread outline). Reduced border radius (tabs rounded-lg to rounded-md, content rounded-xl to rounded-lg). Window controls restyled to match utility buttons (h-8 w-8 rounded-lg, consistent spacing). Animations: sliding tab indicator (background pill animates between tabs, 200ms expo ease-out), EventPanel entrance (scale 0.92->1 + fade from anchor point, 180ms), section expansion (svelte slide transition, 180ms cubicOut on pomodoro/notifications/repeat/music/description), section toggle feedback (SVG icon micro-bounce 1->1.2->1 + background fill transition on active sections), event block selection contour fade-in (box-shadow + ::after opacity transition, 180ms), save flow (button morphs to CircleCheck + "Saved" text, panel scale-down exit 140ms). Smooth theme toggle via View Transition API (GPU-accelerated 250ms crossfade, fallback to instant). ConfirmDialog entrance animation (backdrop fade + dialog scale 0.95->1, 180ms), reduced border radius on dialog and buttons. Softer event block selection contour (1px border at 30% opacity, multi-layer box-shadow glow). Fixed recurring event glow repetition on save: root cause was event-editing class applied by both editing AND preview props, so suppressing editingId alone left glow via previewedIds; fix suppresses both editingId and previewedIds during store mutation window, letting CSS transitions fade all blocks simultaneously. Removed broken flashSavedBlock (was matching by date instead of event ID, only flashing first match, conflicting with CSS transition on box-shadow). |
| 2026-03-25 | Rounded window corners: enabled transparent window background in Tauri config, body background set to transparent, added clip-path inset with 10px border-radius when not maximized. Outer wrapper clips all content including fixed-position overlays (ConfirmDialog, IdleOverlay). isMaximized tracked via Tauri resize listener to disable rounding when maximized (no corner gaps at screen edges). Past event dimming: per-event brightness(0.7) filter on blocks whose end time has passed (computed via effectiveMinuteRange in DayColumn). Cross-midnight event handling: added endDate state to EventPanel with auto-adjustment (syncEndDateFromTimes bumps endDate to next day when endTime < startTime), end date picker UI (compact label + floating datepicker, prevents selecting before startDate), emitChange and buildSaveData use endDate for end datetime. Fixed buildCreateDisplay to prefer panel-provided changes.end over drag preview for cross-midnight creates. Fixed applyFollowing visual compression bug: virtual template end date was reconstructed from instanceEvent.start date instead of instanceEvent.end date, causing cross-midnight events to collapse to MIN_EVENT_HEIGHT (12px). Fix uses instanceEvent.start/end directly. 2 new tests (158 total). iCalendar import prep (Phase A): added 5 new columns to calendar_events (all_day, location, url, transparency, status) via DB migration v2. New calendars table with visibility/read-only/source tracking, default "GanbaruAI" row. TypeScript types extended (EventTransparency, EventStatus, Calendar, PositionedAllDayEvent). New calendars.svelte.ts store (load, toggleVisibility, add, remove, isReadOnly). Calendar event store updated: mapRow, load, addBlock, updateBlock, detachInstance, splitSeries all handle new columns. Utils: eventsForDay excludes all-day, new allDayEventsForDay/allDayEventsForWeek/layoutAllDayEventsForWeek (greedy row-stacking). WeekView/DayView: sticky all-day banners with CSS grid subgrid alignment, colored bars for all-day events. EventPanel: all-day toggle (hides time pickers), location input (MapPin), URL input (ExternalLink), busy/free toggle, status selector (confirmed/tentative/cancelled), read-only mode. EventBlock: dashed border + opacity for free, diagonal stripes for tentative, line-through + low opacity for cancelled, location display. CalendarHeader wired to real calendars store with visibility toggles. CalendarView filters by visible calendars, passes readOnly to EventPanel. Drag controller skips all-day events. 14 new tests (172 total). EventPanel UX redesign: all-day/show-as/status unified into single toolbar card with icon labels (Sun, Eye, CircleCheck), show-as and status as dropdown pickers instead of inline pills. Description/location/URL grouped into one card with consistent icon rows. Description editor restructured: toolbar aligned with editor box, read-only formatted preview when not editing, click-outside to close (no abrupt onblur), in-only slide animation on toolbar, 5-line scroll limit. |
| 2026-03-26 | EventPanel sizing harmony: unified icon sizes (13px), text sizes (11px), and leading across all panel rows (all-day/status toolbar, description/location/URL card, pomodoro/notifications/repeat/music headers). Fixed font vertical centering with translateY(1.13px) nudge on section header text (Inter font ascender/descender asymmetry). Webview zoom: Ctrl+=/- to zoom in/out (0.5x-2.0x range, 0.1 steps), Ctrl+0 to reset, persisted via localStorage, applied at module load (no flash). New zoom.svelte.ts store following theme store pattern. Added core:webview:allow-set-webview-zoom Tauri capability. Description editor UX: removed hover shadow (matches location/URL style), added done button (Check icon), smooth close animation with single persistent DOM element (contenteditable toggling + CSS transitions for background/padding/border-radius, 250ms), toolbar slides independently via Svelte transition:slide, padding transition in parallel with toolbar collapse to prevent overshoot. Default event color changed from sky to slate. Color picker moved from bottom row to compact circle next to title with popover grid (20 colors). Title fade gradient no longer affects color circle (separated DOM structure). Panel drag persistence: panel stays where dragged when editing fields (userDragged flag skips anchor repositioning). Panel viewport clamping: can't go above title bar (minTop) or below viewport bottom. Section expansion: all-day/desc/loc/url hidden with slide animation when section opens, sections and save button always visible. Directional expansion: panel detects available room above/below and expands in one direction only (bottom-pinned via CSS bottom for lower-half panels, top-pinned for upper-half). Jitter-free animations using CSS bottom instead of JS-computed top in pinned mode. Smooth close: pinning maintained during reverse animation with baseTop baking to prevent jumps. Sub-pixel rounding on position values. |
| 2026-03-27 | iCalendar import readiness: structural changes so the calendar can store and display everything Google Calendar, Outlook, and Apple Calendar export without data loss. DB migration v3: source_uid, visibility, priority, categories (JSON), geo (JSON), sequence, rdate (JSON), extended_properties (JSON), organizer (JSON) columns on calendar_events; calendar_event_overrides table (RECURRENCE-ID support); calendar_event_attendees table (normalized); calendar_event_alarms table (VALARM). TypeScript types: EventVisibility, AttendeeRole/Status, AlarmAction, EventAttendee, EventOrganizer, EventAlarm, GeoCoordinates, EventOverride, OrdinalWeekday; RecurrenceConfig extended with ordinalWeekdays, byMonthDay, byMonth, bySetPos, byYearDay, byWeekNo, wkst; CalendarEvent extended with sourceUid, visibility, priority, categories, geo, sequence, rdate, extendedProperties, organizer, attendees, alarms, overrides. Store: extracted map-row.ts (pure mapRow + related table mappers), loadAttendees/loadAlarms/loadOverrides async loaders, addBlock/updateBlock/detachInstance/splitSeries extended for all new columns. RRULE engine: full RFC 5545 BY* rule support in parser/serializer (ordinal BYDAY like 2TU/-1FR, BYMONTHDAY, BYMONTH, BYSETPOS, BYYEARDAY, BYWEEKNO, WKST, UNTIL with datetime precision), formatRecurrenceLabel handles all new patterns. Recurrence expansion: advanceMonthlyOrdinal (Nth weekday), advanceMonthlyByDay (BYMONTHDAY with end-of-month clamping), advanceYearlyOrdinal, advanceYearlyByMonthDay; override merging during expansion; RDATE instance generation with deduplication; isPastUntil with datetime precision. EventPanel UI: visibility picker (Shield/Lock icons, public/private/confidential dropdown), attendees display (expandable list with status dots, organizer badge), monthly repeat sub-options (Day N vs Nth weekday radio toggle), geo coordinates display on location row, RDATE indicator in repeat section. Full attendee lifecycle: add/remove/toggle optional (req-participant vs opt-participant), RSVP status cycling (click dot to cycle needs-action/accepted/tentative/declined), guest permissions (compact icon toggles with labels: Edit/Invite/See guests, positioned above attendee list with border divider, active bg-foreground/10 vs inactive bg-foreground/5 states). DB migration v4: guest_can_modify, guest_can_invite_others, guest_can_see_other_guests columns. GuestPermissions type. Attendee persistence: saveAttendees helper (delete+insert), wired into addBlock/updateBlock/detachInstance/splitSeries. Complete save data flow: onSave type extended with visibility/attendees/guestPermissions, buildSaveData includes all new fields, CalendarView handlePanelSave passes everything to store. Attendee list UX: scrollable after 3 entries (max-h-72px, scrollbar-thin), top/bottom fade masks (dynamic via scroll position tracking + ResizeObserver), tighter row spacing (py-0.5). Smooth scroll: createSmoothScroll utility in utils.ts (RAF lerp with 1px minimum speed to avoid asymptotic stall, damping 0.6, ease 0.15), applied to WeekView, DayView, and attendee list scroll containers (fixes discrete wheel tick jumpiness on Linux). 107 new tests across rrule.test.ts, recurrence.test.ts, map-row.test.ts (279 total). |
| 2026-03-28 | EventPanel refactoring: extracted 8 sub-components + 1 shared utility from EventPanel.svelte (2641 -> 1050 lines, no behavior/UX changes). New files: event-panel-utils.ts (54 lines: bounceIcon, buildCalendarGrid, SHORT_MONTHS, DAY_LETTERS), MiniDatePicker.svelte (158 lines: reusable calendar picker replacing 3 duplicated pickers), TimePicker.svelte (78 lines: scrollable time slot list with duration labels), ColorPicker.svelte (41 lines: color circle + popover grid), DescriptionEditor.svelte (295 lines: rich text editor with toolbar, link popover, capture-phase close-on-outside), AttendeesSection.svelte (229 lines: attendee list with RSVP cycling, guest permissions, scroll fade), PomodoroSection.svelte (149 lines: preset/custom pomodoro config with idle toggle), NotificationsSection.svelte (209 lines: preset + custom notifications with unit dropdowns), RecurrenceSection.svelte (367 lines: frequency/interval/weekday/monthly mode/end conditions with embedded MiniDatePicker). Parent retains section orchestration, mutual exclusion of floating pickers, panel positioning/drag, save aggregation. All components use $bindable for two-way state sync. 0 type errors, 0 behavior changes. Cleanup pass: MiniDatePicker todayStr computed once as $derived instead of 42x new Date() per render, removed unused singular field from RecurrenceSection FREQ_OPTIONS, removed two no-op ternaries in RecurrenceSection end-type buttons. UX polish: smooth scrolling on TimePicker via createSmoothScroll, fixed selected month/year hover contrast in MiniDatePicker and CalendarHeader sidebar (hover:bg-primary/90 instead of generic hover bg). AttendeesSection: close-on-click-outside via capture-phase listener (matching DescriptionEditor pattern), Check/done button in header, no hover highlight when expanded, active:scale-75 press animation on flag/X icons, removed hover:text-foreground from flag to preserve off-state visibility, hover:text-destructive on remove button, 250ms slide transition. EventPanel toolbar: compacted all-day/show-as/status/visibility row (text-[10px], 12px icons, tighter padding) to prevent line wrapping. Past event dimming: light mode now uses saturate(0.8) filter + white ::before overlay (rgba 255,255,255,0.3) instead of brightness(0.7), keeping events fully opaque while lightening them naturally against the white background; dark mode unchanged. Fixed calendar gridlines clipped globally by pomodoro rail: replaced single gridLeft offset with per-line isMinuteInRail check so only lines within a rail segment's vertical range are offset, leaving all other hours full-width. |
| 2026-03-29 | All-day event feature parity: created AllDayEventChip.svelte component (editing glow, status indicators, recurrence/notification badges, past dimming, 2px visual gap via my-px margins, cursor states). New useAllDayDragController.svelte.ts for day-column-based drag-to-move (horizontal), drag-to-resize (edge handles), and drag-to-create in the all-day banner area (click threshold distinguishes click from drag). WeekView all-day banner now always visible (serves as drop/create target), wired to both chip component and drag controller with drag preview and create preview rendering. DayView all-day banner always visible with click-on-empty-space to create all-day events. Edit session extended: CreatePreview gains allDay/endDateStr fields, openCreate accepts allDay flag, display-events.ts buildCreateDisplay sets allDay on template event. CalendarView handleEventCreate accepts optional allDay parameter, EventPanel initialAllDay prop auto-sets toggle on create. MonthView fixed: new allEventsForDay utility returns both all-day (sorted first) and timed events for each day cell (previously eventsForDay filtered out all-day events entirely). AllDayDragState type added to types.ts. EventPanel all-day toggle: stashes/restores original times on toggle, defaults to current-time + 1h when turning off an event that was created as all-day. Event block visual gap: subtracted 2px from rendered height so consecutive events show a legible separator (like Google Calendar) without affecting the timeline rail continuity (clipped-bottom events keep full height). Fixed header wheel scroll leak: added stopPropagation to handleHeaderWheel in WeekView and DayView so scrolling on day name headers only triggers navigation, not vertical scroll. Custom scrollbar: replaced native scrollbar with dedicated gutter column (12px, CalendarScrollbar.svelte) in WeekView and DayView so the last day column is fully clickable; gutter top matches header bg, body uses --background; bottom bar (12px) frames the calendar. Hidden first (0:30) and last (24:00) gridlines for clean edges. Disabled resize handles on clipped edges (cross-midnight events at 00:00/24:00 boundaries). Extended resize handles 5px beyond event block bounds so near-miss clicks trigger resize instead of create (overflow-hidden moved from wrapper to content div). Snap time label flips below line when near sticky header: reactive snapLabelBelow derived from effectiveSnapY vs actual sticky header bottom in viewport coords, works during both hover and drag/resize. Snap line at 24:00 nudged up 2px to stay visible within overflow-hidden container. Snap line follows mouse during scroll: DayColumn listens to scroll parent events and recalculates snap position from stored clientY, using raw pixel position (no grid snap) during scroll for smooth 1:1 tracking; mouse move retains 15-minute grid snap for click precision. Removed isScrolling prop and debounce timer from WeekView/DayView. Removed bottom bar from WeekView/DayView. Window corner rounding: replaced clip-path with border-radius + overflow hidden for smoother anti-aliasing. Light mode theme: replaced blue-tinted palette with neutral warm grays inspired by dark mode's charcoal undertone (sidebar #DCDCE2, background #F4F4F7, primary #404048, muted-foreground #646470, today circle #38383F); all borders/inputs neutralized from blue-gray to pure gray; calendar variables updated to match. Fixed theme transition transparency: old view-transition snapshot now stays fully opaque (animation: none) so the window never becomes see-through during light/dark crossfade. Event move drag: events can now cross midnight in both directions by vertical dragging alone; when the event fully crosses midnight, targetDate shifts forward/backward and rawStart wraps so the event seamlessly transfers to the adjacent day (previously start was clamped to 0, blocking upward midnight crossing and preventing cross-midnight events from being dragged fully to the other day). All-day banner UX polish: empty column areas in banner clickable for event creation (column targets span all rows at z-index 0 behind events), long titles truncate (min-width: 0 on grid wrappers), day header text vertically aligned with timezone label (click-to-create strip absolutely positioned so it doesn't offset flex centering), keyed {#each} loops for all-day events (matching DayColumn pattern), computed banner height replaces bind:clientHeight to prevent ResizeObserver-induced layout jitter, click handling aligned with timed events (AllDayEventChip handles clicks directly via native click event, drag controller only handles actual drags - removed onEventClick from controller config). |
| 2026-03-30 | All-day drag improvements: vertical reordering of stacked all-day events (drag up/down to change row order, sort order persisted via extendedProperties.allDaySortOrder, layoutAllDayEventsForWeek uses sort order as tiebreaker for same-duration same-start events, reorderEvents computes new sort orders for all overlapping events on drop). Drag preview no longer shows editing glow (replaced AllDayEventChip with plain outlined div matching timed event drag preview pattern in DayColumn). Click suppression after drag (didDrag flag prevents event panel from opening when releasing after a move). Drag flicker fix (state cleared after awaiting onEventUpdate so original chip stays hidden until data reflects new position). Event title defaults: removed "Focus session" fallback from buildSaveData (events save with empty title), create preview and drag preview show "(No title)" at 50% opacity with time range (matching EventBlock pattern), EventPanel shows empty input with placeholder on re-edit. Removed EventPanel entrance animation (instant appearance). Create-by-drag preview uses editing glow effect (box-shadow + 30% contour) instead of thick 1.5px outline. Save button disabled state: cursor-not-allowed and click guard when no changes. Click-through backdrop: clicking an event block while a panel is open switches directly to that event instead of requiring two clicks (backdrop peeks underneath via elementFromPoint). Scroll while panel open: wheel events forwarded through backdrop to calendar underneath. Faster save animation (200ms flash + 80ms fade). Toggle panel: re-clicking the same event block closes the panel (delegates to handlePanelClose for dirty check). Calendar event block polish: thin vertical separation (2px via calc width reduction) between horizontally stacked events (same pattern as existing 2px bottom gap for continuous events). Bottom gap now conditional (hasEventBelow flag computed from startMinute set intersection, only subtracted when another event starts where this one ends). Added overflow-hidden on event block wrapper so inner content respects rounded corners. Always-reserved rail space: event container and gridlines always offset by railWidth+4 regardless of pomodoro events, eliminating position jump when adding/removing pomodoro. Pomodoro rail preview during creation: default pomodoroConfig set in edit session openCreate, propagated to pseudo-event via buildCreateDisplay, rail and break bands visible immediately when create panel opens. Pomodoro rail preview during move: rail/timeline computation excludes dragging event and includes drag preview event, so rail follows the preview position with planned bands. EventPanel column-based positioning: anchor computed from day column rect (horizontal) + event block rect (vertical) instead of event block alone, so panel clears the entire column including rail (fixes panel overlapping progress bar on stacked events). |
