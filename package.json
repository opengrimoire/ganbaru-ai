{
  "name": "ganbaru-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:native-host": "cargo build --bin ganbaru-ai-native-messaging",
    "setup:chromium-extension": "node apps/client/scripts/setup-chromium-extension.mjs",
    "setup:brave-extension": "node apps/client/scripts/setup-brave-extension.mjs",
    "audit:deps": "pnpm audit --audit-level low",
    "audit:rust": "cargo audit",
    "audit": "pnpm -w run audit:deps && pnpm -w run audit:rust",
    "check:rust": "cargo fmt --check && cargo clippy --workspace -j 1 -- -D warnings",
    "check:rust:ci": "cargo fmt --check && cargo clippy --workspace -j 2 -- -D warnings",
    "check:frontend": "turbo check --concurrency=1",
    "check": "pnpm -w run check:rust && pnpm -w run check:frontend",
    "test:rust": "cargo test --workspace -j 1 -- --test-threads=1",
    "test:rust:ci": "cargo test --workspace -j 2 -- --test-threads=1",
    "test:frontend": "turbo test --concurrency=1 -- --maxWorkers=1 --exclude=\"src/lib/benchmark/**/*.test.ts\" --shard=1/4 && turbo test --concurrency=1 -- --maxWorkers=1 --exclude=\"src/lib/benchmark/**/*.test.ts\" --shard=2/4 && turbo test --concurrency=1 -- --maxWorkers=1 --exclude=\"src/lib/benchmark/**/*.test.ts\" --shard=3/4 && turbo test --concurrency=1 -- --maxWorkers=1 --exclude=\"src/lib/benchmark/**/*.test.ts\" --shard=4/4",
    "test": "pnpm -w run test:rust && pnpm -w run test:frontend",
    "test:benchmark-contracts:rust": "cargo test -p ganbaru-tauri-app --lib -j 1 music::library::fixtures::tests::dense_fixture_is_complete_deterministic_and_repeatable -- --ignored --test-threads=1",
    "test:benchmark-contracts:frontend": "pnpm --dir apps/client exec vitest run src/lib/benchmark --maxWorkers=1",
    "test:benchmark-contracts": "pnpm -w run test:benchmark-contracts:rust && pnpm -w run test:benchmark-contracts:frontend",
    "editor-check": "turbo editor-check --concurrency=1",
    "bundle-contracts": "turbo bundle-contracts --concurrency=1",
    "validate": "pnpm -w run check:rust && pnpm -w run test:rust && pnpm -w run check:frontend && pnpm -w run test:frontend && pnpm -w run editor-check && pnpm -w run bundle-contracts",
    "validate:ci": "pnpm -w run check:rust:ci && pnpm -w run test:rust:ci && pnpm -w run check:frontend && pnpm -w run test:frontend && pnpm -w run editor-check && pnpm -w run bundle-contracts",
    "validate:full": "pnpm -w run audit && pnpm -w run validate"
  },
  "devDependencies": {
    "turbo": "^2.9.14",
    "typescript": "6.0.3"
  },
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": "^22.12.0 || ^24.0.0",
    "pnpm": ">=11 <12"
  }
}
